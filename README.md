# dd_chain_explorer — FROZEN (tombstone)

This repository is retired. Nothing is committed here again; it is kept only as
history and is scheduled for deletion by the operator (v0.6.0 T-V.5 / T-D.3).

Its successors:

| Concern | Repository |
|---|---|
| `specs/` (authoritative since 2026-09-24, C-DAY) and the Databricks bundle `job_market_data` | [`dd-chain-explorer`](https://github.com/marcoaureliomenezes/dd-chain-explorer) |
| All infrastructure (Terraform, CI lanes) | `dd-chain-infrastructure` |
| Capture images | `dd-chain-capture` |

The `specs/` tree here is frozen at `f5fb9bc`; the live tree is in `dd-chain-explorer`.

# DynamoDB — DEV environment
# Substitui Redis local (ElastiCache em PROD → DynamoDB em DEV/PROD)

DYNAMODB_TABLE=dm-chain-explorer

# App mode: dev = credenciais ~/.aws / prod = IAM task role
APP_ENV=dev

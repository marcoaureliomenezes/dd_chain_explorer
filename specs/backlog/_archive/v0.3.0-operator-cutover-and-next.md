# Backlog — CANDIDATE — v0.3.1/next: OIDC cutover completion + deferred items

> **Status:** CANDIDATE — NOT picked into any release.
> **Created:** 2026-06-11 (at v0.3.0 CLOSURE, by operator "ship now, defer
> operator-pending work" decision — deferral authority recorded in
> `specs/_archive/releases/v0.3.0/CLOSURE.md` §Deferred/§Drifts).
> **Owner:** project-manager (backlog curator).
> **Source:** v0.3.0 CLOSURE deferrals + code-reviewer non-blocking findings +
> sdd-drift-audit memory gaps GAP-LD-2..6.
> **Interim accepted risks until done:** exposed Infura key valid in historical logs
> (ADR-R6-2); static AWS keys remain the CI auth (OP-R6-4 pending).

---

## WS-1 — Operator cutover actions (verification tasks, ordered)

Each item is DONE only when its evidence criterion is recorded (handoff or run URL).

| # | Item | Action | Evidence criterion |
|---|---|---|---|
| 1 | OP-R6-1 Infura key rotation | Rotate the exposed Infura API key(s); update SSM `/dm-chain-explorer/infura-api-keys` | New key IDs live in SSM; old key disabled at provider — until then ACCEPTED RISK per ADR-R6-2 |
| 2 | OP-R6-2 OIDC provider | Create the GitHub OIDC identity provider (`token.actions.githubusercontent.com`) in account 016098071081 (one-time) | Provider ARN recorded |
| 3 | B2 apply | Apply `services/prd/03_iam` (4 OIDC roles; first apply rides the bootstrap static-key path per ADR-R6-6); set repo vars `AWS_DEPLOY_ROLE_{DEV,HML,PRD,READONLY}` | `terraform apply` output + the 4 role ARNs set as repo vars |
| 4 | A3 / OP-R6-3 hml required_reviewers | `gh api -X PUT repos/:owner/:repo/environments/hml -f "reviewers[][type]=User" -F "reviewers[][id]=42839553"` | `gh api repos/:owner/:repo/environments` shows required_reviewers on `hml` |
| 5 | B3(f) 4-role assumption evidence | Run the non-mutating `aws sts get-caller-identity` evidence steps under EACH of the 4 roles (dev/hml/prd deploy + read-only, incl. the `hml-apps` sub-claim) | 4 run URLs + assumed-role ARNs recorded BEFORE OP-R6-4 |
| 6 | B3(e) live OIDC validation | One PR-plan run + one dev deploy + one drift run, all green under OIDC | 3 green run URLs |
| 7 | A2(f) live hml graduation | One full green hml run through the new informed-gate flow BEFORE the first prd run | Run URL + artifact list + pre-gate per-stack summary evidence |
| 8 | OP-R6-4 static-key deletion | Delete/deactivate the static IAM access keys + GitHub repo secrets — ONLY after #5 evidence | Keys deleted in IAM; secrets removed from repo settings |

## WS-2 — Code-reviewer non-blocking cleanups (from v0.3.0 rc ship review)

| Item | Location | Summary |
|---|---|---|
| Dead `local root=` assignments | `scripts/ci/deploy_env.sh:275,318` | Remove dead variable assignments |
| Stale comment | `scripts/ci/plan_env.sh:112-114` | Comment no longer matches the implemented flow |
| PowerUserAccess on deploy roles | `services/prd/03_iam` | Tighten to least-privilege policies — fold into epic WS-D (single stack tree / provider pinning) |

## WS-3 — Memory gaps (GAP-LD-2..6, from sdd-drift-audit)

GAP-LD-1 (CI/CD atom) was closed at v0.3.0 CLOSURE (`specs/memory/cicd-pipeline.md`).
GAP-LD-2..6 remain: per the audit's living-documentation gap list (environments/HML
topology depth, Makefile/dev-tooling surface, DABs bundle inventory depth, runbook/
operational procedures, test-strategy atom wiring). Author as memory atoms in a future
DEFINITION/CLOSURE window; cross-check against
`specs/audits/20260611T001412Z-cb56f84c/sdd-drift-audit.md`.

## Pointer — the big epic

`specs/backlog/platform-audit-remediation-20260611.md` remains **CANDIDATE**: v0.3.0
consumed only the WS-A + WS-B1/B2/B3 slice. Still open there: WS-B4/B5/B6
(encryption-at-rest CMK, Databricks token in TF state, low-sev hardening), WS-C (stale
PRD Databricks monolith / HML-validates-PRD / Makefile retirement), WS-D (single stack
tree / provider pinning), WS-E (capture-layer deprecation ADR), WS-F (memory fidelity
rewrite incl. architecture.md, test CI wiring), WS-G.

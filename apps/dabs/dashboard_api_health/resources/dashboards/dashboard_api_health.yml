resources:
  dashboards:
    api_health:
      display_name: "API Health"
      # Generated from the sibling .lvdash.json.tmpl by
      # ../../../render_dashboard_templates.sh --catalog {dev|hml|prd}
      # (run before validate/deploy — dataset SQL is not bundle-templated).
      file_path: ../../src/dashboards/04_api_health.lvdash.json
      warehouse_id: ${var.warehouse_id}
      embed_credentials: false
      parent_path: /Shared/dd-chain-explorer/dashboards

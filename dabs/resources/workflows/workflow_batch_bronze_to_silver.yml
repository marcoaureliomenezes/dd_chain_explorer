resources:
  jobs:
    workflow_batch_bronze_to_silver:
      name: "[${bundle.target}] dm-batch-bronze-to-silver"
      tasks:
        - task_key: bronze_to_silver_contracts_txs
          notebook_task:
            notebook_path: ../../src/batch/batch_contracts/2_bronze_to_silver_contracts_txs.py
            source: WORKSPACE
            base_parameters:
              catalog:   ${var.catalog}
              exec_date: "1970-01-01T00"
      tags:
        layer:   silver
        project: dd-chain-explorer
